<!DOCTYPE html>
<html>
  <head>
    <title><%= @title == nil ? 'Ziggie - Urlshortener, Click Tracker, Url Analytics, Custom Links' : @title %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'https://js.stripe.com/v3/' %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf-8" >
    <meta http-equiv="X-UA-Compatible" content="IE=Edge">
    <% @meta[:description].blank? ? @meta.merge!({:description => "Create and share trusted, powerful links for your business. Track your links and get data about how people interract with your links. Learn from statistics and grow your business"}) : ''%>
    <%= meta_tag(@meta) %>
  </head>

  <body>
      <%= render 'layouts/nav' %>
      <div class='container-fluid'>
      <% flash.each do |key,msg| %>
        <div class='alert alert-warning'>
            <%= msg %>
        </div>
      <% end %>
      
      <%= yield %>
      </div>
      <%= render 'layouts/footer'%>
  </body>
</html>

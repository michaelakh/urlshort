<p id="notice"><%= notice %></p>

<h1>Urls</h1>

<table>
  <thead>
    <tr>
      <th>Slug</th>
      <th>Visits</th>
      <th>Snapshot</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @urls.each do |url| %>
      <tr>
        <td><%= url.slug %></td>
        <td><%= url.visits %></td>
        <td><%= url.snapshot %></td>
        <td><%= link_to 'Show', url %></td>
        <td><%= link_to 'clic', edit_url_path(url) %></td>
        <td><%= link_to 'Destroy', url, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Url', new_url_path %>
